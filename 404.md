---
title: 404 — Page Not Found
description: 页面走丢了。回到首页继续探索吧。
---

<div class="notfound">
  <p class="eyebrow nf-eyebrow">404 · NOT FOUND</p>
  <h1 class="nf-code">4·0·4</h1>
  <p class="nf-title">页面走丢了</p>
  <p class="nf-sub">这里好像什么都没有。要不回去看看别的？</p>
  <div class="nf-actions">
    <a class="nf-btn primary" href="./">← 返回首页</a>
    <a class="nf-btn ghost" href="#knowledge">🌎 Knowledge</a>
    <a class="nf-btn ghost" href="skillmap.html">📊 能力图谱</a>
  </div>
</div>

<style>
:root {
  --bg: #ffffff;
  --bg-elev: #f6f6f7;
  --border: #e5e5e7;
  --text: #0a0a0a;
  --text-dim: #6b6b6b;
  --accent: #6366F1;
  --accent-2: #818cf8;
  --accent-soft: rgba(99,102,241,0.08);
  --font-sans: ui-sans-serif, -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  --font-mono: ui-monospace, 'SF Mono', Menlo, Consolas, monospace;
}
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0a0a0a;
    --bg-elev: #141416;
    --border: #262626;
    --text: #ededee;
    --text-dim: #8a8a8e;
    --accent: #818cf8;
    --accent-2: #a5b4fc;
    --accent-soft: rgba(129,140,248,0.12);
  }
}
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: var(--font-sans);
  background: var(--bg);
  color: var(--text);
  font-size: 15px;
  line-height: 1.65;
  min-height: 100vh;
}
.notfound {
  max-width: 560px;
  margin: 0 auto;
  padding: 9rem 1.25rem 4rem;
  text-align: center;
}
.nf-eyebrow {
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: var(--accent);
  font-weight: 600;
  margin-bottom: .9rem;
}
.nf-code {
  font-size: clamp(4.5rem, 18vw, 7rem);
  font-weight: 800;
  line-height: 1;
  letter-spacing: -.06em;
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-2) 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 1rem;
}
.nf-title { font-size: 1.4rem; font-weight: 700; letter-spacing: -.02em; margin-bottom: .5rem; }
.nf-sub { color: var(--text-dim); margin-bottom: 2rem; }
.nf-actions {
  display: flex; justify-content: center;
  flex-wrap: wrap;
  gap: .6rem;
}
.nf-btn {
  display: inline-flex; align-items: center;
  padding: .55rem 1.1rem;
  border-radius: 8px;
  font-weight: 600;
  font-size: 13.5px;
  border: 1px solid transparent;
  transition: background .15s, color .15s, transform .1s, border-color .15s;
}
.nf-btn.primary { background: var(--accent); color: #fff; }
.nf-btn.primary:hover { background: var(--accent-2); color: #fff; transform: translateY(-1px); text-decoration: none; }
.nf-btn.ghost { background: transparent; color: var(--accent); border-color: var(--accent); }
.nf-btn.ghost:hover { background: var(--accent-soft); color: var(--accent); transform: translateY(-1px); text-decoration: none; }
</style>
